{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit:*)",
        "hooks": [
          {
            "type": "command",
            "command": "npx prettier -w app src tests && npm run lint -f && npx tsc --noEmit"
          }
        ]
      }
    ],
    "PostToolUse": [],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "npx prettier -w app src tests && npm run lint -f && npx tsc --noEmit"
          }
        ]
      }
    ]
  }
}