/**
 * Inbox Tab Screen
 *
 * Displays all recent items across all lists with grouping and sorting.
 * Implemented as US3.1 - Inbox as Hub Central.
 */

import type { ReactElement } from 'react';

import { InboxScreen } from '@presentation/screens';

export default function InboxTab(): ReactElement {
  return <InboxScreen />;
}
