/**
 * Lists Tab Screen
 *
 * Displays all user lists grouped by category (Notes, Shopping, Movies, Books, Games).
 * Implemented as US3.2 - Tela Listas com Agrupamento por Categoria.
 */

import type { ReactElement } from 'react';

import { ListsScreen } from '@presentation/screens';

export default function ListsTab(): ReactElement {
  return <ListsScreen />;
}
