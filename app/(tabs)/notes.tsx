/**
 * Notes Tab Screen
 *
 * Single prefabricated notes list with configurable layout.
 * Displays all note items grouped by date with sorting controls.
 */

import type { ReactElement } from 'react';

import { NotesScreen } from '@presentation/screens';

export default function NotesTab(): ReactElement {
  return <NotesScreen />;
}
