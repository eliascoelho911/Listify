/**
 * Search Tab Screen
 *
 * Search interface with filters, history, and results highlighting.
 * Implements US3.4 - Advanced Search Screen.
 */

import type { ReactElement } from 'react';

import { SearchScreen } from '@presentation/screens';

export default function SearchTab(): ReactElement {
  return <SearchScreen />;
}
